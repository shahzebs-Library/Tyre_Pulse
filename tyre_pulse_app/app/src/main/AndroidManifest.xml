<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:name=".TyrePulseApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tyre_pulse_app">

        <!--
            TyrePulseApp implements androidx.work.Configuration.Provider and supplies a
            HiltWorkerFactory, i.e. WorkManager is initialised ON DEMAND. The default
            initializer must therefore be removed, and this is not merely a lint
            preference: left in place it initialises WorkManager with the DEFAULT worker
            factory before Hilt's is available, and every @HiltWorker then fails to
            instantiate at runtime - including the periodic sync started in onCreate,
            which is what drains the offline queue.

            THE WHOLE PROVIDER GOES, NOT JUST WORKMANAGER'S META-DATA. Removing only
            the androidx.work.WorkManagerInitializer meta-data was tried first and lint
            still failed: RemoveWorkManagerInitializer keys on the PRESENCE of
            androidx.startup.InitializationProvider, not on its children. This is also
            the form the WorkManager documentation prescribes.

            The cost, checked rather than assumed: this removes every androidx.startup
            initializer, not only WorkManager's. Nothing in this app references
            ProcessLifecycleOwner, EmojiCompat, ProfileInstaller or androidx.startup,
            and none of those is a declared dependency - so the only ones that could be
            merged in arrive transitively and degrade quietly (a baseline profile not
            installed, newer emoji not backfilled). If a library is ever added that
            genuinely needs its initializer, re-add InitializationProvider here with
            just that initializer's meta-data rather than reverting this.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tyre_pulse_app"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
