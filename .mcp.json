{
  "mcpServers": {
    "magic": {
      "command": "npx",
      "args": ["-y", "@21st-dev/magic@latest"],
      "env": {
        "API_KEY": "${MAGIC_API_KEY}"
      }
    },
    "codebase-memory": {
      "command": "${HOME}/.local/bin/codebase-memory-mcp",
      "args": []
    }
  }
}
