# Tyre Pulse — Roadmap / Unfinished Work (as of 2026-08-19)

Pointer for the next agent. Nothing here is broken-blocking; these are the open
decisions, carried bugs, and owner/ops actions collected from `PROJECT_MEMORY.md`.
Read `PROJECT_MEMORY.md` first — it has the full reasoning behind each item.
Next free migration: **V604**. Branch: `claude/accident-builder-report-ui-2bkwb5` (== origin/main).

---

## 1. OWNER / OPS actions (not code — cannot be done from here)
- **NO MOBILE BUILD is a standing owner instruction.** All mobile work since versionCode 43
  (V601 saved-signature, etc.) is merged but on NOBODY's phone until the owner asks for an EAS build.
- **Play release when the owner is ready:** smoke-test 1.6.0 on a real phone (inspection w/ photos,
  scanner, a checklist — first build with R8 shrinking, failure modes are invisible until a device runs it);
  remove versionCode 41 from Play Production; promote Play Closed -> Production for 1.6.0; THEN set
  `system_config.mobile_latest_version` = 1.6.0 (leave `mobile_min_version` at 1.3.2 until a tester confirms).
- **Assign the trade accounts** in Console -> Users, Title Case: Mechanic, Electrician, Driver,
  Maintenance Supervisor, Workshop Supervisor. Until then the two workshop checklists reach only the 5
  oversight users.
- **Enable Supabase leaked-password protection** (Auth dashboard).
- **Realtime re-measure under daytime load** — only if the owner reports slowness again (see PENDING item 1).

## 2. OWNER DECISIONS — data/policy (do NOT decide unilaterally; PENDING block in PROJECT_MEMORY)
- **Stop auditing bulk imports?** 8 import days = 94.5% of `audit_log_v2`. Lever: V499 `app.bulk_import` guard.
  Changes the audit contract.
- **Apply V579 audit-log country attribution?** A KSA-only Manager reads 31,618 audit rows naming UAE/Egypt.
  The fix HIDES history (a KSA admin loses 39,979 rows). Exact SQL ready in `MIGRATIONS_V579_*.sql`. Not applied.
- **Shorten `audit_retention_days` 365 -> 90?** Deletes 0 rows today, bounds it going forward.
- **Egypt Director org membership** + `get_email_by_identifier` anon email oracle (free mitigations: rename the
  super-admin username, enable 2FA). Carried, unchanged.

## 3. OPEN CODE BUGS (carried, deliberately left — each has a reason in PROJECT_MEMORY)
- **`tyre_records.serial_no` case-split partial-scrap (43 tyres).** Scrapping `K507B403590` leaves the `k...`
  row Active. DO NOT fix by normalising the column — mobile barcode `lookupTyreBySerial` + `tyreExchange.findTyreBySerial`
  are case-sensitive `.eq()`, so uppercasing turns a split-history bug into a can't-find-in-the-field bug.
  **Cheap first move (no client change):** make `scrap_tyre_by_serial` / `unscrap_tyre_by_serial` /
  `list_scrapped_tyres` join case-insensitively. (PENDING item 12.)
- **`failureRate` prints 0.0%** when nothing is rated — should read N/A. Same honest-null class already fixed elsewhere.
- **820 UAE rows carry a tyre BRAND in `removal_reason`** (ROADX/FIREMAX/... , all UAE, `brand` already correct).
  Cosmetic to reporting but pollutes any removal-reason analysis. Owner decision (clearing a column is semantic).
  V590 left it untouched on purpose.
- **`SerialTracker.exportBulkExcel` exports `bulkResults`, not `filteredBulkResults`** — export ignores the active
  filter. (Agent-flagged, not acted on.)
- **`ComplianceDashboard.filteredTyres` uses strict country equality** while newer scopings are null-safe
  (`applyCountry` convention). Owner decision — could hide/show null-country rows differently. (Agent-flagged.)
- **Web `src/pages/Inspections.jsx` checklist tab has NO tyre-completeness gate** — the engine is available and
  mobile enforces it; the web tab was never wired (its agent was cut off). Port the mobile gate.

## 4. MOBILE polish (needs the deferred EAS build to matter)
- `mobile/components/PhotoCapture.tsx` menu strings are hard-coded English (gallery picking exists in every
  language but reads English).
- `profileStale` is exposed but nothing renders the "working offline" hint.
- `SignaturePad`'s `value` prop is not passed by its 4 non-checklist call sites (meter-logs, inspection/new,
  both approval screens) — the "reopen shows blank pad, Clear erases the others" hazard still exists there.

## 5. HOW TO WORK HERE (short version)
- Push straight to `main`; **batch and push ONCE** at the end (every push to main starts a production build).
- Snapshot before any destructive data change (`_bak.<name>`); measure first; state the row/money delta.
- Verify a guard with a rolled-back impersonation test, then mutation-test it.
- Keep user-facing strings ASCII (no em/en dashes, `->` not arrows).
- `vercel.json` `ignoreCommand` skips the build for docs/mobile/`.sql`-only pushes.
